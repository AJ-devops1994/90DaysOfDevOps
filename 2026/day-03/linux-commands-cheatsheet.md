
#Process management
$ps -a
$ps -aux
$
$
$

#file system and disk
$df-h : Show disk usage by filesystem
$du -sh <dir path> : Get size of a directory
$
$
$

#Networking commands(network troubleshooting):
$sudo apt install net-tools : to use networking commands flawlessly
$nslookup <dna_name>
$dig <dns_name> or <ip>
$ip addr
$ipconfig 
$ping <dns_name> or <ip>
$curl
